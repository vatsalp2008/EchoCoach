# ─────────────────────────────────────────────────────────────
# EchoCoach environment. Copy to .env and fill in secrets.
# Cognee reads these on `import cognee`, so .env must sit at the
# repo root (or the dir Python is launched from).
# ─────────────────────────────────────────────────────────────

# ── Auth (email+password; JWT session cookie) ──
# Strong random secret for signing session JWTs. REQUIRED in production —
# rotating it logs everyone out. Generate: python -c "import secrets;print(secrets.token_urlsafe(48))"
SESSION_SECRET=
SESSION_TTL_DAYS=14
# Google Sign-In (ID-token flow). Backend verifies the token's audience against
# GOOGLE_CLIENT_ID; the same value must ALSO be exposed to the frontend as
# NEXT_PUBLIC_GOOGLE_CLIENT_ID in frontend/.env.local (Next reads env from its
# own dir). The client SECRET is not used by the ID-token flow. Add
# http://localhost:3000 (and your prod origin) as an Authorized JavaScript origin
# on the OAuth client, or the button errors with "origin not allowed".
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# The session cookie is SameSite=None; Secure (needed for the split frontend/API
# origins). HTTPS is required in production; http://localhost is exempt (Chrome
# treats it as a secure context), so no extra setup is needed for local dev.

# ── App LLM (grading + debrief), used by backend/app/llm_client.py ──
# Use a standard AI Studio API key (starts with "AIza"). Keys shaped "AQ.*" are
# quota-restricted (~20 requests/day) — avoid.
APP_LLM_PROVIDER=gemini
GEMINI_API_KEY=
APP_LLM_MODEL=gemini-2.5-flash

# ── Cognee internal LLM (cognify/recall/improve) — Gemini (see docs/DECISIONS ADR-011) ──
# Local Ollama models fail cognee's structured-output extraction and are minutes-slow,
# so graph-building runs on Gemini too. Uses a standard AIza key's free tier.
LLM_PROVIDER=gemini
LLM_MODEL=gemini/gemini-2.5-flash-lite
LLM_API_KEY=

# ── Cognee embeddings — local fastembed (in-process, no key, no Ollama) ──
# `pip install fastembed`. Cognee uses its default local model (BAAI/bge-small).
EMBEDDING_PROVIDER=fastembed

# ── Cognee local stack (defaults; stated explicitly for clarity) ──
VECTOR_DB_PROVIDER=lancedb
GRAPH_DATABASE_PROVIDER=kuzu
DB_PROVIDER=sqlite

# ── Phase 3: external grounding ──
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USER_AGENT=echocoach:v1.0 (by /u/your_username)
GITHUB_TOKEN=

# ── Login PINs (2 known users, see backend/app/auth.py) ──
# Change these from the demo defaults if this ever leaves your machine.
PIN_VATSAL=1111
PIN_SAKSHI=2222
